using UnityEngine;

public class Rotation : MonoBehaviour
{
    public GameObject cube;
    public float rotationSpeed;
    void Update() {
        //cube.transform.Rotate(0, 1 * Time.deltaTime * rotationSpeed, 0);
    }
}